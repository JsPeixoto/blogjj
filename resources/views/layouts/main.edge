<!DOCTYPE html>
@!component('components/html')
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>{{title}}</title>
        @entryPointStyles('app')
    </head>
    <body>

        <div class="navbar absolute bg-base-200 h-16">

            <div class="flex-none lg:hidden">
                <label for="my-drawer" class="btn btn-square btn-ghost">

                </label>
            </div>

            <div class="flex-1">
              <a href="{{route('home.index')}}" class="btn btn-ghost normal-case text-xl">Blog JJ</a>
            </div>

            <div class="flex-none p-1">
                <a class="normal-case font-medium"> {{auth.user.name}} </a>
            </div>

            <div class="flex-none gap-2">
                <div class="dropdown dropdown-end">
                    <label tabindex="0" class="btn btn-ghost btn-circle avatar">
                        <div class="w-10 rounded-full">
                            <img src="" />
                        </div>
                    </label>
                    <ul tabindex="0" class="mt-3 p-2 shadow menu menu-compact dropdown-content bg-base-100 rounded-box w-52">
                        <li>
                            <a class="justify-between" href="{{ route('user.show') }}">
                              Perfil
                            </a>
                        </li>
                        <li><a href="{{ route('auth.destroy') }}">Sair</a></li>
                    </ul>
                </div>
            </div>

        </div>

        <div class="drawer drawer-mobile pt-16">
            <input id="my-drawer" type="checkbox" class="drawer-toggle" />

            <div class="drawer-content flex flex-col p-4">

                @!section('body')

            </div>

            <div class="drawer-overlay">
            <label for="my-drawer" class="drawer-overlay"></label>
            <ul class="menu p-4 w-80 bg-base-200 text-base-content">
                <li><a href="{{ route('home.index') }}" class="active">Página inicial</a></li>
                <li></li>
                <a href="{{ route('post.create') }}" class="btn btn-primary" >Postar</a>
            </ul>
            </div>
        </div>

    </body>

</html>
